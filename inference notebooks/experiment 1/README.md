# Experiment 1 Inference Notebooks

This folder contains the initial inference notebooks used for the first evaluation run.

## Purpose

These notebooks were used to run the initial model-judge evaluations and produce the CSV files now stored under:

```text
results /experiment 1
```

The first experiment focused on running multiple judge models across the AMP evaluation criteria and collecting outputs for model agreement analysis.

## Notebooks

- `LlaMa.ipynb`
- `gemma.ipynb`
- `qwen_4b.ipynb`
- `qwen_8b.ipynb`

## Notes

These notebooks are kept for provenance and reproducibility of the initial run. New prompt-format experiment code lives under:

```text
scripts/prompt_experiment
```
